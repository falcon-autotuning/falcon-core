- comment_raw: "/**\n   * @brief Construct a Domain.\n   * @param min_val Minimum\
    \ value of the domain.\n   * @param max_val Maximum value of the domain.\n   *\
    \ @param lesser_bound_contained Whether the minimum value is included in the\n\
    \   * domain.\n   * @param greater_bound_contained Whether the maximum value is\
    \ included in the\n   * domain.\n   * @throws std::invalid_argument if min_val\
    \ is greater than max_val.\n   */"
  comment: '@brief Construct a Domain.

    @param min_val Minimum value of the domain.

    @param max_val Maximum value of the domain.

    @param lesser_bound_contained Whether the minimum value is included in the

    domain.

    @param greater_bound_contained Whether the maximum value is included in the

    domain.

    @throws std::invalid_argument if min_val is greater than max_val.'
  declaration: "Domain(double min_val,\n         double max_val,\n         bool  \
    \ lesser_bound_contained  = true,\n         bool   greater_bound_contained = true);"
  kind: function
  name: falcon_core::math::domains::Domain
- comment_raw: "/**\n   * @brief Construct a Domain from a pair of bounds.\n   * @param\
    \ bounds A pair of doubles representing the minimum and maximum\n   * values.\n\
    \   * @param lesser_bound_contained Whether the minimum value is included in the\n\
    \   * domain.\n   * @param greater_bound_contained Whether the maximum value is\
    \ included in the\n   * domain.\n   */"
  comment: '@brief Construct a Domain from a pair of bounds.

    @param bounds A pair of doubles representing the minimum and maximum

    values.

    @param lesser_bound_contained Whether the minimum value is included in the

    domain.

    @param greater_bound_contained Whether the maximum value is included in the

    domain.'
  declaration: "Domain(std::pair<double, double> bounds,\n         bool          \
    \            lesser_bound_contained  = true,\n         bool                  \
    \    greater_bound_contained = true);"
  kind: function
  name: falcon_core::math::domains::Domain
- comment_raw: "/**\n   * @brief Get the lesser bound of the domain.\n   * @return\
    \ The lesser bound.\n   */"
  comment: '@brief Get the lesser bound of the domain.

    @return The lesser bound.'
  declaration: const double lesser_bound() const;
  kind: function
  name: falcon_core::math::domains::Domain::lesser_bound
- comment_raw: "/**\n   * @brief Get the greater bound of the domain.\n   * @return\
    \ The greater bound.\n   */"
  comment: '@brief Get the greater bound of the domain.

    @return The greater bound.'
  declaration: const double greater_bound() const;
  kind: function
  name: falcon_core::math::domains::Domain::greater_bound
- comment_raw: "/**\n   * @brief Get the bounds of the domain as a pair.\n   * @return\
    \ A pair of doubles representing the lesser and greater bounds.\n   */"
  comment: '@brief Get the bounds of the domain as a pair.

    @return A pair of doubles representing the lesser and greater bounds.'
  declaration: const std::pair<double, double> bounds() const;
  kind: function
  name: falcon_core::math::domains::Domain::bounds
- comment_raw: "/**\n   * @brief Check if the lesser bound is contained in the domain.\n\
    \   * @return True if the lesser bound is contained, false otherwise.\n   */"
  comment: '@brief Check if the lesser bound is contained in the domain.

    @return True if the lesser bound is contained, false otherwise.'
  declaration: const bool lesser_bound_contained() const;
  kind: function
  name: falcon_core::math::domains::Domain::lesser_bound_contained
- comment_raw: "/**\n   * @brief Check if the greater bound is contained in the domain.\n\
    \   * @return True if the greater bound is contained, false otherwise.\n   */"
  comment: '@brief Check if the greater bound is contained in the domain.

    @return True if the greater bound is contained, false otherwise.'
  declaration: const bool greater_bound_contained() const;
  kind: function
  name: falcon_core::math::domains::Domain::greater_bound_contained
- comment_raw: "/**\n   * @brief Check if a value is in the domain.\n   * @param value\
    \ The value to check.\n   * @return True if the value is in the domain, false\
    \ otherwise.\n   */"
  comment: '@brief Check if a value is in the domain.

    @param value The value to check.

    @return True if the value is in the domain, false otherwise.'
  declaration: const bool in(double value) const;
  kind: function
  name: falcon_core::math::domains::Domain::in
- comment_raw: "/**\n   * @brief Get the size of the domain.\n   * @return The size\
    \ of the domain.\n   */"
  comment: '@brief Get the size of the domain.

    @return The size of the domain.'
  declaration: const double range() const;
  kind: function
  name: falcon_core::math::domains::Domain::range
- comment_raw: "/**\n   * @brief Get the center of the domain.\n   * @return The center\
    \ of the domain.\n   */"
  comment: '@brief Get the center of the domain.

    @return The center of the domain.'
  declaration: const double get_center() const;
  kind: function
  name: falcon_core::math::domains::Domain::get_center
- comment_raw: "/**\n   * @brief Get the intersection of this domain with another\
    \ domain.\n   * @param other The other domain.\n   * @return A new domain representing\
    \ the intersection.\n   * @throws std::runtime_error if the domains do not intersect.\n\
    \   */"
  comment: '@brief Get the intersection of this domain with another domain.

    @param other The other domain.

    @return A new domain representing the intersection.

    @throws std::runtime_error if the domains do not intersect.'
  declaration: "union of this domain with another domain.\n   * @param other The other\
    \ domain.\n   * @return A new domain representing the union.\n   * @throws std::runtime_error\
    \ if the domains do not overlap or touch.\n   */\n  const std::shared_ptr<Domain>\
    \ operator|(\n      const std::shared_ptr<Domain>& other) const;"
  kind: union
  name: null
- comment_raw: "/**\n   * @brief Check if the domain is empty.\n   * @return True\
    \ if the domain is empty, false otherwise.\n   */"
  comment: '@brief Check if the domain is empty.

    @return True if the domain is empty, false otherwise.'
  declaration: const bool is_empty() const;
  kind: function
  name: falcon_core::math::domains::Domain::is_empty
- comment_raw: "/**\n   * @brief Check if this domain completely contains another\
    \ domain.\n   * @param other The other domain.\n   * @return True if this domain\
    \ contains the other domain, false otherwise.\n   */"
  comment: '@brief Check if this domain completely contains another domain.

    @param other The other domain.

    @return True if this domain contains the other domain, false otherwise.'
  declaration: const bool contains_domain(const std::shared_ptr<Domain>& other) const;
  kind: function
  name: falcon_core::math::domains::Domain::contains_domain
- comment_raw: "/**\n   * @brief Shift the domain by an offset.\n   * @param offset\
    \ The offset to shift by.\n   * @return A new domain representing the shifted\
    \ domain.\n   */"
  comment: '@brief Shift the domain by an offset.

    @param offset The offset to shift by.

    @return A new domain representing the shifted domain.'
  declaration: const std::shared_ptr<Domain> shift(double offset) const;
  kind: function
  name: falcon_core::math::domains::Domain::shift
- comment_raw: "/**\n   * @brief Scale the domain by a factor.\n   * @param factor\
    \ The factor to scale by.\n   * @return A new domain representing the scaled domain.\n\
    \   */"
  comment: '@brief Scale the domain by a factor.

    @param factor The factor to scale by.

    @return A new domain representing the scaled domain.'
  declaration: const std::shared_ptr<Domain> scale(double factor) const;
  kind: function
  name: falcon_core::math::domains::Domain::scale
- comment_raw: "/**\n   * @brief Calculate the linear transformation parameters to\
    \ map this domain to\n   * another domain.\n   * @param other The other domain\
    \ to map to.\n   * @return A pair of doubles representing the scale and offset\
    \ of the\n   * transformation.\n   */"
  comment: '@brief Calculate the linear transformation parameters to map this domain
    to

    another domain.

    @param other The other domain to map to.

    @return A pair of doubles representing the scale and offset of the

    transformation.'
  declaration: "const std::pair<double, double> calculate_transform(\n      const\
    \ std::shared_ptr<Domain>& other) const;"
  kind: function
  name: falcon_core::math::domains::Domain::calculate_transform
- comment_raw: "/**\n   * @brief Transform a value from this domain to another domain.\n\
    \   * @param other The other domain to transform to.\n   * @param value The value\
    \ to transform.\n   * @return The transformed value.\n   */"
  comment: '@brief Transform a value from this domain to another domain.

    @param other The other domain to transform to.

    @param value The value to transform.

    @return The transformed value.'
  declaration: "const double transform(const std::shared_ptr<Domain>& other,\n   \
    \                      double                         value) const;"
  kind: function
  name: falcon_core::math::domains::Domain::transform
