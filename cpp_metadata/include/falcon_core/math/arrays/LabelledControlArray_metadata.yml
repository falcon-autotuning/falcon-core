- comment_raw: "/**\n * @file LabelledControlArray.hpp\n * @brief Defines the a control\
    \ array with a label.\n */"
  comment: '@file LabelledControlArray.hpp

    @brief Defines the a control array with a label.'
  declaration: 'class LabelledControlArray : public ControlArray, public IsLabelled<double>
    {'
  kind: class
  name: null
- comment_raw: "/**\n   * @brief Construct a new Labelled Control Array object.\n\
    \   * @param array The control array.\n   * @param label The acquisition context\
    \ label.\n   */"
  comment: '@brief Construct a new Labelled Control Array object.

    @param array The control array.

    @param label The acquisition context label.'
  declaration: "LabelledControlArray(\n      const arrays::ControlArraySP&       \
    \                        array,\n      const autotuner_interfaces::contexts::AcquisitionContextSP&\
    \ label);"
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray
- comment_raw: "/**\n   * @brief Another constructor for a new Labelled Control Array\
    \ object using a\n   * BaseArray.\n   * @param array The control array.\n   *\
    \ @param label The acquisition context label.\n   */"
  comment: '@brief Another constructor for a new Labelled Control Array object using
    a

    BaseArray.

    @param array The control array.

    @param label The acquisition context label.'
  declaration: "LabelledControlArray(\n      const generic::FArraySP<double>&    \
    \                        array,\n      const autotuner_interfaces::contexts::AcquisitionContextSP&\
    \ label);"
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray
- comment_raw: "/**\n   * @brief Constructs a new LabelledControlArray object using\
    \ a BaseArray.\n   * @param array The control array.\n   * @param port The instrument\
    \ port to create the label from.\n   */"
  comment: '@brief Constructs a new LabelledControlArray object using a BaseArray.

    @param array The control array.

    @param port The instrument port to create the label from.'
  declaration: "LabelledControlArray(\n      const generic::FArraySP<double>&    \
    \                  array,\n      const instrument_interfaces::names::InstrumentPortSP&\
    \ port);"
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray
- comment_raw: "/**\n   * @brief Return a new Array with the given shape.\n   * @param\
    \ shape The new shape.\n   * @return A reshaped LabelledControlArray.\n   */"
  comment: '@brief Return a new Array with the given shape.

    @param shape The new shape.

    @return A reshaped LabelledControlArray.'
  declaration: "std::shared_ptr<LabelledControlArray> reshape(\n      const std::vector<size_t>&\
    \ shape) const;"
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray::reshape
- comment_raw: "/**\n   * @brief Flip the data along the given axis.\n   * @param\
    \ axis The axis to flip.\n   * @return A flipped LabelledControlArray.\n   */"
  comment: '@brief Flip the data along the given axis.

    @param axis The axis to flip.

    @return A flipped LabelledControlArray.'
  declaration: std::shared_ptr<LabelledControlArray> flip(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray::flip
- comment_raw: "/**\n   * @brief Return the gradient of the data along a given axis.\n\
    \   *\n   * Computes the gradient along the specified axis using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @param axis The axis to compute the gradient.\n\
    \   * @return The gradient FArray.\n   */"
  comment: '@brief Return the gradient of the data along a given axis.

    Computes the gradient along the specified axis using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @param axis The axis to compute the gradient.

    @return The gradient FArray.'
  declaration: generic::FArraySP<double> gradient(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray::gradient
- comment_raw: "/**\n   * @brief Return the gradient of the data along all axes.\n\
    \   *\n   * Computes the gradient for each axis of the array using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @return A vector of FArray gradients (one\
    \ for each axis).\n   */"
  comment: '@brief Return the gradient of the data along all axes.

    Computes the gradient for each axis of the array using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @return A vector of FArray gradients (one for each axis).'
  declaration: generic::ListSP<generic::FArray<double>> gradient() const;
  kind: function
  name: falcon_core::math::arrays::LabelledControlArray::gradient
