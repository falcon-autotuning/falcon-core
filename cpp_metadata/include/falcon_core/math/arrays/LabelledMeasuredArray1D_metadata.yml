- comment_raw: "/**\n * @file LabelledMeasuredArray1D.hpp\n * @brief Defines the LabelledMeasuredArray1D\
    \ class for FalconCore.\n */"
  comment: '@file LabelledMeasuredArray1D.hpp

    @brief Defines the LabelledMeasuredArray1D class for FalconCore.'
  declaration: "class LabelledMeasuredArray1D : public LabelledMeasuredArray,\n  \
    \                              public Is1D<double> {"
  kind: class
  name: null
- comment_raw: "/**\n   * @brief Construct a new Labelled Measured Array object.\n\
    \   * @param array The control array.\n   * @param label The acquisition context\
    \ label.\n   */"
  comment: '@brief Construct a new Labelled Measured Array object.

    @param array The control array.

    @param label The acquisition context label.'
  declaration: "LabelledMeasuredArray1D(\n      const arrays::MeasuredArraySP&   \
    \                           array,\n      const autotuner_interfaces::contexts::AcquisitionContextSP&\
    \ label);"
  kind: function
  name: falcon_core::math::arrays::for::LabelledMeasuredArray1D
- comment_raw: "/**\n   * @brief Construct a new Labelled Measured Array object.\n\
    \   * @param array The control array.\n   * @param label The acquisition context\
    \ label.\n   */"
  comment: '@brief Construct a new Labelled Measured Array object.

    @param array The control array.

    @param label The acquisition context label.'
  declaration: "LabelledMeasuredArray1D(\n      const arrays::MeasuredArray1DSP& \
    \                           array,\n      const autotuner_interfaces::contexts::AcquisitionContextSP&\
    \ label);"
  kind: function
  name: falcon_core::math::arrays::for::LabelledMeasuredArray1D
- comment_raw: "/**\n   * @brief Construct a new Labelled Measured Array object.\n\
    \   * @param array The control array.\n   * @param label The acquisition context\
    \ label.\n   */"
  comment: '@brief Construct a new Labelled Measured Array object.

    @param array The control array.

    @param label The acquisition context label.'
  declaration: "LabelledMeasuredArray1D(\n      const generic::FArraySP<double>& \
    \                           array,\n      const autotuner_interfaces::contexts::AcquisitionContextSP&\
    \ label);"
  kind: function
  name: falcon_core::math::arrays::for::LabelledMeasuredArray1D
- comment_raw: "/**\n   * @brief Return a new Array with the given shape.\n   * @param\
    \ shape The new shape.\n   * @return A reshaped LabelledMeasuredArray1D.\n   */"
  comment: '@brief Return a new Array with the given shape.

    @param shape The new shape.

    @return A reshaped LabelledMeasuredArray1D.'
  declaration: "std::shared_ptr<LabelledMeasuredArray1D> reshape(\n      const std::vector<size_t>&\
    \ shape) const;"
  kind: function
  name: falcon_core::math::arrays::for::reshape
- comment_raw: "/**\n   * @brief Flip the data along the given axis.\n   * @param\
    \ axis The axis to flip.\n   * @return A flipped LabelledMeasuredArray1D.\n  \
    \ */"
  comment: '@brief Flip the data along the given axis.

    @param axis The axis to flip.

    @return A flipped LabelledMeasuredArray1D.'
  declaration: std::shared_ptr<LabelledMeasuredArray1D> flip(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::for::flip
- comment_raw: "/**\n   * @brief Return the gradient of the data along a given axis.\n\
    \   *\n   * Computes the gradient along the specified axis using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @param axis The axis to compute the gradient.\n\
    \   * @return The gradient FArray.\n   */"
  comment: '@brief Return the gradient of the data along a given axis.

    Computes the gradient along the specified axis using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @param axis The axis to compute the gradient.

    @return The gradient FArray.'
  declaration: generic::FArraySP<double> gradient(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::for::gradient
- comment_raw: "/**\n   * @brief Return the gradient of the data along all axes.\n\
    \   *\n   * Computes the gradient for each axis of the array using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @return A vector of FArray gradients (one\
    \ for each axis).\n   */"
  comment: '@brief Return the gradient of the data along all axes.

    Computes the gradient for each axis of the array using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @return A vector of FArray gradients (one for each axis).'
  declaration: generic::ListSP<generic::FArray<double>> gradient() const;
  kind: function
  name: falcon_core::math::arrays::for::gradient
