- comment_raw: "/**\n * @file ControlArray1D.hpp\n * @brief Defines a 1dimensional\
    \ indpendant control vector for falcon.\n */"
  comment: '@file ControlArray1D.hpp

    @brief Defines a 1dimensional indpendant control vector for falcon.'
  declaration: 'class ControlArray1D : public ControlArray, public Is1D<double> {'
  kind: class
  name: null
- comment_raw: "/**\n   * @brief Return the minimum value of the array.\n   */"
  comment: '@brief Return the minimum value of the array.'
  declaration: double min() const;
  kind: function
  name: falcon_core::math::arrays::ControlArray1D::min
- comment_raw: "/**\n   * @brief Return the maximum value of the array.\n   */"
  comment: '@brief Return the maximum value of the array.'
  declaration: double max() const;
  kind: function
  name: falcon_core::math::arrays::ControlArray1D::max
- comment_raw: "/**\n   * @brief Return a new Array with the given shape.\n   * @param\
    \ shape The new shape.\n   * @return A reshaped ControlArray1D.\n   */"
  comment: '@brief Return a new Array with the given shape.

    @param shape The new shape.

    @return A reshaped ControlArray1D.'
  declaration: "std::shared_ptr<ControlArray1D> reshape(\n      const std::vector<size_t>&\
    \ shape) const;"
  kind: function
  name: falcon_core::math::arrays::ControlArray1D::reshape
- comment_raw: "/**\n   * @brief Flip the data along the given axis.\n   * @param\
    \ axis The axis to flip.\n   * @return A flipped ControlArray1D1D.\n   */"
  comment: '@brief Flip the data along the given axis.

    @param axis The axis to flip.

    @return A flipped ControlArray1D1D.'
  declaration: std::shared_ptr<ControlArray1D> flip(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::ControlArray1D::flip
- comment_raw: "/**\n   * @brief Return the gradient of the data along a given axis.\n\
    \   *\n   * Computes the gradient along the specified axis using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @param axis The axis to compute the gradient.\n\
    \   * @return The gradient FArray.\n   */"
  comment: '@brief Return the gradient of the data along a given axis.

    Computes the gradient along the specified axis using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @param axis The axis to compute the gradient.

    @return The gradient FArray.'
  declaration: generic::FArraySP<double> gradient(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::ControlArray1D::gradient
- comment_raw: "/**\n   * @brief Return the gradient of the data along all axes.\n\
    \   *\n   * Computes the gradient for each axis of the array using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @return A vector of FArray gradients (one\
    \ for each axis).\n   */"
  comment: '@brief Return the gradient of the data along all axes.

    Computes the gradient for each axis of the array using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @return A vector of FArray gradients (one for each axis).'
  declaration: generic::ListSP<generic::FArray<double>> gradient() const;
  kind: function
  name: falcon_core::math::arrays::ControlArray1D::gradient
