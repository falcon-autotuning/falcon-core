- comment_raw: "/**\n * @file Discretizer.hpp\n * @brief Defines the Discretizer class\
    \ for FalconCore.\n */"
  comment: '@file Discretizer.hpp

    @brief Defines the Discretizer class for FalconCore.'
  declaration: enum class Dividers {
  kind: enum
  name: null
- comment_raw: "/**\n * @brief Abstract base class for discretizers.\n *\n * @details\n\
    \ * Discretizers define how a domain is divided into discrete steps.\n */"
  comment: '@brief Abstract base class for discretizers.

    @details

    Discretizers define how a domain is divided into discrete steps.'
  declaration: 'class Discretizer : public generic::Song {'
  kind: class
  name: null
- comment_raw: "/**\n   * @brief Construct a Discretizer. The delta must be within\
    \ the\n   * delta_domain.\n   * @param delta Step size.\n   * @param delta_domain\
    \ Domain for step size.\n   * @param type the type of Dividers to construct.\n\
    \   */"
  comment: '@brief Construct a Discretizer. The delta must be within the

    delta_domain.

    @param delta Step size.

    @param delta_domain Domain for step size.

    @param type the type of Dividers to construct.'
  declaration: "Discretizer(const double&            delta,\n              const domains::DomainSP&\
    \ delta_domain,\n              const Dividers&          type);"
  kind: function
  name: falcon_core::math::discrete_spaces::for::Discretizer
- comment_raw: "/**\n   * @brief Constructs a CartesianDiscretizer for Cartesian square\
    \ axes.\n   */"
  comment: '@brief Constructs a CartesianDiscretizer for Cartesian square axes.'
  declaration: static std::shared_ptr<Discretizer> CartesianDiscretizer(const double&
    delta);
  kind: function
  name: falcon_core::math::discrete_spaces::for::CartesianDiscretizer
- comment_raw: "/**\n   * @brief Construct a PolarDiscretizer for polar angled coordinates.\n\
    \   */"
  comment: '@brief Construct a PolarDiscretizer for polar angled coordinates.'
  declaration: static std::shared_ptr<Discretizer> PolarDiscretizer(const double&
    delta);
  kind: function
  name: falcon_core::math::discrete_spaces::for::PolarDiscretizer
- comment_raw: "/**\n   * @brief Checks if the current Discretizer is Cartesian.\n\
    \   */"
  comment: '@brief Checks if the current Discretizer is Cartesian.'
  declaration: const bool is_cartesian() const;
  kind: function
  name: falcon_core::math::discrete_spaces::for::is_cartesian
- comment_raw: "/**\n   * @brief Checks if the current Discretizer is Polar.\n   */"
  comment: '@brief Checks if the current Discretizer is Polar.'
  declaration: const bool is_polar() const;
  kind: function
  name: falcon_core::math::discrete_spaces::for::is_polar
