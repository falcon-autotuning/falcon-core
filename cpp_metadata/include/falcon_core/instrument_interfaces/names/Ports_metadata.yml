- comment_raw: "/**\n   * @brief Initialize ports from a collection of port.\n   */"
  comment: '@brief Initialize ports from a collection of port.'
  declaration: Ports(const std::vector<InstrumentPortSP> ports);
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports
- comment_raw: "/**\n   * @brief Initialize ports from a collection of port.\n   */"
  comment: '@brief Initialize ports from a collection of port.'
  declaration: Ports(const generic::ListSP<InstrumentPort> ports);
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports
- comment_raw: "/**\n   * @brief return the collection of ports.\n   */"
  comment: '@brief return the collection of ports.'
  declaration: generic::ListSP<InstrumentPort> ports() const;
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::ports
- comment_raw: "/**\n   * @brief Return the default names of the ports.\n   */"
  comment: '@brief Return the default names of the ports.'
  declaration: generic::ListSP<std::string> get_default_names() const;
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::get_default_names
- comment_raw: "/**\n   * @brief Return the pseudo names of the ports.\n   * @throws\
    \ std::runtime_error if any port does not have a pseudo name.\n   */"
  comment: '@brief Return the pseudo names of the ports.

    @throws std::runtime_error if any port does not have a pseudo name.'
  declaration: "generic::ListSP<physics::device_structures::Connection> get_pseudo_names()\n\
    \      const;"
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::get_pseudo_names
- comment_raw: "/**\n   * @brief Return the raw string names of the ports.\n   */"
  comment: '@brief Return the raw string names of the ports.'
  declaration: generic::ListSP<std::string> _get_raw_names() const;
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::_get_raw_names
- comment_raw: "/**\n   * @brief Gets a llist of names to satisfy an instrument interface.\n\
    \   */"
  comment: '@brief Gets a llist of names to satisfy an instrument interface.'
  declaration: generic::ListSP<std::string> _get_instrument_facing_names() const;
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::_get_instrument_facing_names
- comment_raw: "/**\n   * @brief Check if any port has the given pseudo name.\n  \
    \ * @param name The pseudo name to check for.\n   * @return The port with the\
    \ given name\n   * @throws std::runtime_error if no port has the given name.\n\
    \   */"
  comment: '@brief Check if any port has the given pseudo name.

    @param name The pseudo name to check for.

    @return The port with the given name

    @throws std::runtime_error if no port has the given name.'
  declaration: "InstrumentPortSP _get_psuedoname_matching_port(\n      const physics::device_structures::ConnectionSP&\
    \ name) const;"
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::_get_psuedoname_matching_port
- comment_raw: "/**\n   * @brief Check if any port has the given instrument type.\n\
    \   * @param type The instrument type to check for.\n   * @return The port with\
    \ the given instrument type.\n   * @throws std::runtime_error if no port has the\
    \ given type.\n   */"
  comment: '@brief Check if any port has the given instrument type.

    @param type The instrument type to check for.

    @return The port with the given instrument type.

    @throws std::runtime_error if no port has the given type.'
  declaration: "InstrumentPortSP _get_instrument_type_matching_port(\n      const\
    \ Instrument& type) const;"
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::_get_instrument_type_matching_port
- comment_raw: "/**\n   * @brief Check if the ports contains only knobs.\n   * @return\
    \ True if the ports only contain knobs.\n   */"
  comment: '@brief Check if the ports contains only knobs.

    @return True if the ports only contain knobs.'
  declaration: const bool is_knobs() const;
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::is_knobs
- comment_raw: "/**\n   * @brief Check if the ports contains only meters.\n   * @return\
    \ True if the ports only contain meters.\n   */"
  comment: '@brief Check if the ports contains only meters.

    @return True if the ports only contain meters.'
  declaration: const bool is_meters() const;
  kind: function
  name: falcon_core::instrument_interfaces::names::Ports::is_meters
