# falcon-core

C-api compatible version of the main repository for falcon autotuning.

## Install from source

In order to build this c package you must first build the original cpp repository

Then navigate to the c-api folder and run

```console
make all
```

The expected build .so binary should be findable at ../dist/c_api/
