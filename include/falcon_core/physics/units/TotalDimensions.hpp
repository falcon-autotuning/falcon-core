#pragma once

#include <map>
#include <string>
namespace falcon_core {
namespace physics {
namespace units {
using TotalDimensions = std::map<std::string, int>;
}
}  // namespace physics
}  // namespace falcon_core
