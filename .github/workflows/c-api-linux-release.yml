name: C-API Linux Release Build

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build-release:
    name: Build C-API Release (Linux)
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create build user
        run: |
          useradd -m builduser
          echo "builduser ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
          chown -R builduser:builduser .

      - name: Cache pacman packages
        uses: actions/cache@v4
        with:
          path: /var/cache/pacman/pkg
          key: ${{ runner.os }}-pacman-${{ github.ref }}
          restore-keys: |
            ${{ runner.os }}-pacman-

      - name: Cache AUR packages
        uses: actions/cache@v4
        with:
          path: |
            packaging/highfive/*.pkg.tar.zst
            packaging/exprtk/*.pkg.tar.zst
          key: ${{ runner.os }}-aur-${{ github.ref }}
          restore-keys: |
            ${{ runner.os }}-aur-

      - name: Install system dependencies
        run: |
          pacman -Syu --noconfirm
          pacman -S --noconfirm \
            base-devel \
            git \
            wget \
            curl \
            pkg-config \
            openssh \
            cereal \
            hdf5 \
            boost \
            bzip2 \
            expat \
            nlohmann-json \
            openssl \
            python \
            sqlite \
            yaml-cpp \
            zlib \
            ninja \
            llvm \
            ccache \
            clang \
            glibc \
            cmake \
            lld

      - name: Set clang as default compiler
        run: |
          grep -q '^CC=' /etc/makepkg.conf && \
          sed -i 's|^CC=.*|CC=/usr/bin/clang|' /etc/makepkg.conf || \
          echo 'CC=/usr/bin/clang' >> /etc/makepkg.conf

          grep -q '^CXX=' /etc/makepkg.conf && \
          sed -i 's|^CXX=.*|CXX=/usr/bin/clang++|' /etc/makepkg.conf || \
          echo 'CXX=/usr/bin/clang++' >> /etc/makepkg.conf

      - name: Cache ccache

        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ${{ runner.os }}-ccache-${{ github.ref }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-ccache-${{ github.ref }}-
            ${{ runner.os }}-ccache-

      - name: Install AUR packages
        run: |
          mkdir -p packaging
          chown builduser:builduser packaging
          runuser -u builduser -- env PATH=/usr/bin:$PATH bash -c "
              if [ ! -d packaging/highfive ]; then git clone https://aur.archlinux.org/highfive.git packaging/highfive; fi
              if [ ! -d packaging/exprtk ]; then git clone https://aur.archlinux.org/exprtk.git packaging/exprtk; fi
              cd packaging/highfive
              if ls *.pkg.tar.zst 1> /dev/null 2>&1; then
                sudo pacman -U --noconfirm ./*.pkg.tar.zst
              else
                PKGDEST=\"\$(pwd)\" makepkg -si --noconfirm --needed
              fi
              cd ../exprtk
              if ls *.pkg.tar.zst 1> /dev/null 2>&1; then
                sudo pacman -U --noconfirm ./*.pkg.tar.zst
              else
                PKGDEST=\"\$(pwd)\" makepkg -si --noconfirm --needed
              fi
            "

      - name: Build xtensor packages
        run: |
          runuser -u builduser -- env PATH=/usr/bin:$PATH bash -c "
            cd packaging/xtl && makepkg -si --noconfirm --syncdeps --cleanbuild
            cd ../xsimd && makepkg -si --noconfirm --syncdeps --cleanbuild
            cd ../xtensor && makepkg -si --noconfirm --syncdeps --cleanbuild
            cd ../xtensor-io && makepkg -si --noconfirm --syncdeps --cleanbuild
          "

      - name: Download C++ release build
        uses: actions/download-artifact@v4
        with:
          name: cpp-linux-release-build
          path: cpp/build

      - name: Verify C++ library
        run: |
          test -f cpp/build/libfalcon_core_cpp.so || (echo "ERROR: libfalcon_core_cpp.so not found!" && exit 1)
          file cpp/build/libfalcon_core_cpp.so
          echo "✓ C++ library verified"

      - name: Build C-API library (OPTIMIZED RELEASE)
        run: |
          cd c-api
          CC=/usr/bin/clang CXX=/usr/bin/clang++ CORE_ROOT=../cpp make build

      - name: Verify release build
        run: |
          test -f c-api/build/libfalcon_core_c_api.so || (echo "ERROR: libfalcon_core_c_api.so not found!" && exit 1)
          file c-api/build/libfalcon_core_c_api.so
          size c-api/build/libfalcon_core_c_api.so
          echo "✓ C-API release build verified"

      - name: Package release artifact
        run: |
          cd c-api
          mkdir -p release-package/lib
          mkdir -p release-package/include

          cp build/libfalcon_core_c_api.so release-package/lib/
          cp ../cpp/build/libfalcon_core_cpp.so release-package/lib/
          cp -r include/falcon_core release-package/include/

          cat > release-package/README.md << 'EOF'
          # Falcon Core C-API Library - Linux x64

          ## Contents

          - `lib/libfalcon_core_c_api.so` - C-API shared library
          - `lib/libfalcon_core_cpp.so` - C++ core library (dependency)
          - `include/falcon_core/` - Public C-API header files

          ## Installation

          Copy the libraries and headers to your system:

          ```bash
          sudo cp lib/*.so /usr/local/lib/
          sudo cp -r include/falcon_core /usr/local/include/
          sudo ldconfig
          ```

          ## Dependencies

          Required system packages (same as C++ library):
          - boost (filesystem, system, uuid)
          - hdf5
          - yaml-cpp
          - cereal
          - xtensor
          - xtensor-io
          - xsimd
          - xtl

          Install via your package manager before using this library.

          ## Build Information

          - Platform: Linux x86_64
          - Compiler: Clang/LLVM
          - C Standard: C99
          - C++ Standard: C++14
          - Build Type: Release (Optimized)

          ## Note

          Both libfalcon_core_c_api.so and libfalcon_core_cpp.so are required.
          EOF

          echo "falcon-core-c-api-linux-x64" > release-package/VERSION
          echo "Built: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> release-package/VERSION
          echo "Commit: $GITHUB_SHA" >> release-package/VERSION

          tar -czf falcon-core-c-api-linux-x64.tar.gz -C release-package .
          ls -lh falcon-core-c-api-linux-x64.tar.gz

      - name: Generate checksum
        run: |
          cd c-api
          sha256sum falcon-core-c-api-linux-x64.tar.gz > falcon-core-c-api-linux-x64.tar.gz.sha256
          cat falcon-core-c-api-linux-x64.tar.gz.sha256

      - name: Upload release package
        uses: actions/upload-artifact@v4
        with:
          name: falcon-core-c-api-linux-x64
          path: |
            c-api/falcon-core-c-api-linux-x64.tar.gz
            c-api/falcon-core-c-api-linux-x64.tar.gz.sha256
          retention-days: 90
          compression-level: 0

      - name: Setup tmate session on failure
        if: failure()
        uses: mxschmitt/action-tmate@v3
        timeout-minutes: 30
