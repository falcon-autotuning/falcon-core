- comment_raw: "/**\n * @brief Elements of the config that pertain to an individual\
    \ group of gates.\n */"
  comment: '@brief Elements of the config that pertain to an individual group of gates.'
  declaration: 'class Group : public StandardConfigConnections {'
  kind: class
  name: null
- comment_raw: "/**\n   * @brief Holds information about a group of gates.\n   * @param\
    \ name The name of the group.\n   * @param num_dots The number of dots in the\
    \ group.\n   * @param screening_gates The screening gates in the group.\n   *\
    \ @param reservoir_gates The reservoir gates in the group.\n   * @param plunger_gates\
    \ The plunger gates in the group.\n   * @param barrier_gates The barrier gates\
    \ in the group.\n   * @param order The order of the gates in the group.\n   */"
  comment: '@brief Holds information about a group of gates.

    @param name The name of the group.

    @param num_dots The number of dots in the group.

    @param screening_gates The screening gates in the group.

    @param reservoir_gates The reservoir gates in the group.

    @param plunger_gates The plunger gates in the group.

    @param barrier_gates The barrier gates in the group.

    @param order The order of the gates in the group.'
  declaration: "Group(const autotuner_interfaces::names::ChannelSP& name,\n      \
    \  const int&                                    num_dots,\n        const device_structures::ConnectionsSP&\
    \       screening_gates,\n        const device_structures::ConnectionsSP&    \
    \   reservoir_gates,\n        const device_structures::ConnectionsSP&       plunger_gates,\n\
    \        const device_structures::ConnectionsSP&       barrier_gates,\n      \
    \  const device_structures::ConnectionsSP&       order);"
  kind: function
  name: falcon_core::physics::config::core::Group
- comment_raw: "/**\n   * @brief collect the ohmics pertaining to this group.\n  \
    \ */"
  comment: '@brief collect the ohmics pertaining to this group.'
  declaration: const device_structures::ConnectionsSP ohmics() const;
  kind: function
  name: falcon_core::physics::config::core::Group::ohmics
- comment_raw: "/**\n   * @brief collect the name of this group.\n   */"
  comment: '@brief collect the name of this group.'
  declaration: const autotuner_interfaces::names::ChannelSP& name() const;
  kind: function
  name: falcon_core::physics::config::core::Group::name
- comment_raw: "/**\n   * @brief collect the number of dots in this group.\n   */"
  comment: '@brief collect the number of dots in this group.'
  declaration: const int& num_dots() const;
  kind: function
  name: falcon_core::physics::config::core::Group::num_dots
- comment_raw: "/**\n   * @brief collect the order of the gates in this group.\n \
    \  */"
  comment: '@brief collect the order of the gates in this group.'
  declaration: const geometries::GateGeometryArray1DSP& order() const;
  kind: function
  name: falcon_core::physics::config::core::Group::order
- comment_raw: "/**\n   * @brief Validates if this channel is present.\n   * @param\
    \ channel The channel to validate.\n   * @returns true if the channel is present.\n\
    \   */"
  comment: '@brief Validates if this channel is present.

    @param channel The channel to validate.

    @returns true if the channel is present.'
  declaration: bool has_channel(const autotuner_interfaces::names::ChannelSP& channel)
    const;
  kind: function
  name: falcon_core::physics::config::core::Group::has_channel
- comment_raw: "/**\n   * @brief Checks if this channel could be a charge sensor.\n\
    \   * @returns true if the channel has a single dot.\n   */"
  comment: '@brief Checks if this channel could be a charge sensor.

    @returns true if the channel has a single dot.'
  declaration: bool is_charge_sensor() const;
  kind: function
  name: falcon_core::physics::config::core::Group::is_charge_sensor
- comment_raw: "/**\n   * @brief Gets all of the gates assocciated with this channel\
    \ from the\n   * config.\n   * @param channel The channel to collect the gates\
    \ for.\n   * @returns The gates associated with this channel. If the channel is\
    \ not\n   * correct then empty gates are returned.\n   */"
  comment: '@brief Gets all of the gates assocciated with this channel from the

    config.

    @param channel The channel to collect the gates for.

    @returns The gates associated with this channel. If the channel is not

    correct then empty gates are returned.'
  declaration: "device_structures::ConnectionsSP get_all_channel_gates(\n      const\
    \ autotuner_interfaces::names::ChannelSP& channel) const;"
  kind: function
  name: falcon_core::physics::config::core::Group::get_all_channel_gates
