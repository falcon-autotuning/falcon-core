- comment_raw: "/**\n * @file ControlArray.hpp\n * @brief Defines the ControlArray\
    \ for control independant axis data for falcon.\n */"
  comment: '@file ControlArray.hpp

    @brief Defines the ControlArray for control independant axis data for falcon.'
  declaration: 'class ControlArray : public generic::FArray<double>, public IsControl<double>
    {'
  kind: class
  name: null
- comment_raw: "/**\n   * @brief Return a new Array with the given shape.\n   * @param\
    \ shape The new shape.\n   * @return A reshaped ControlArray.\n   */"
  comment: '@brief Return a new Array with the given shape.

    @param shape The new shape.

    @return A reshaped ControlArray.'
  declaration: std::shared_ptr<ControlArray> reshape(const std::vector<size_t>& shape)
    const;
  kind: function
  name: falcon_core::math::arrays::ControlArray::reshape
- comment_raw: "/**\n   * @brief Flip the data along the given axis.\n   * @param\
    \ axis The axis to flip.\n   * @return A flipped ControlArray.\n   */"
  comment: '@brief Flip the data along the given axis.

    @param axis The axis to flip.

    @return A flipped ControlArray.'
  declaration: std::shared_ptr<ControlArray> flip(size_t axis) const;
  kind: function
  name: falcon_core::math::arrays::ControlArray::flip
- comment_raw: "/**\n   * @brief Return the gradient of the data along a given axis.\n\
    \   *\n   * Computes the gradient along the specified axis using finite differences:\n\
    \   * - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2\n\
    \   * - For boundary points, uses forward (first element) or backward (last\n\
    \   * element) difference.\n   *\n   * @param axis The axis to compute the gradient.\n\
    \   * @return The gradient FArray.\n   */"
  comment: '@brief Return the gradient of the data along a given axis.

    Computes the gradient along the specified axis using finite differences:

    - For interior points, uses central difference: (f(x+1) - f(x-1)) / 2

    - For boundary points, uses forward (first element) or backward (last

    element) difference.

    @param axis The axis to compute the gradient.

    @return The gradient FArray.'
  declaration: "using finite differences:\n   * - For interior points, uses central\
    \ difference: (f(x+1) - f(x-1)) / 2\n   * - For boundary points, uses forward\
    \ (first element) or backward (last\n   * element) difference.\n   *\n   * @return\
    \ A vector of FArray gradients (one for each axis).\n   */\n  generic::ListSP<generic::FArray<double>>\
    \ gradient() const override;"
  kind: alias
  name: null
